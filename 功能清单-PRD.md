# 灵镜 · 一站式 AIGC 创作平台 — 功能清单 / PRD

> 版本 v1.0 · 2026-06 · 状态:核心已上线,持续迭代
> 本文档是产品功能规格,作为开发、报价、私有化交付的依据。原型见 `prototype/`。
> 注:项目已从最初的「数字人 SaaS」扩展为**多模态 AIGC 平台**(影片/图片/音频),本文据当前形态校准。

---

## 0. 产品定位与约束

| 项 | 内容 |
|---|---|
| 产品 | 灵镜 · 一站式 AIGC 内容创作 SaaS 平台 |
| 目标客户 | 融媒体机构(电视台、报社、广电、政企宣传)与内容团队 |
| 核心价值 | 一个平台产出影片/图片/音频三类 AIGC 内容;数字人口播是其中旗舰能力 |
| 技术路线 | 能力全部走**云端 API**(阿里百炼为主,火山方舟为辅),不自建 GPU |
| 交付形态 | **双模式**:①托管(开账号、按量计费);②私有化(整套部署到客户内网) |
| 界面形态 | 左侧工具导航的「系统」+ 公开营销落地页 |

### 0.1 关键架构原则:能力网关层(贯穿全平台)

所有云端能力调用必须经**统一能力网关层**抽象。业务/UI 只声明"生成数字人视频""文生图""克隆音色",不直接绑定厂商。

- 托管:网关后接平台云账号(百炼/火山),计费走平台账户,向客户按量透传。
- 私有化:网关后可换客户自己的云账号 key 或指定厂商。
- 收益:**一套代码,两种交付**;某厂商额度耗尽/故障可切换。
- 落地:provider key **AES-256-GCM 加密入库**(MASTER_KEY),admin 只见 last4;库内密文优先,回落 .env。

---

## 1. 已上线能力总览

### 创作工具(8 类 job,三模态)

| 模态 | 工具 | 后端 job type |
|---|---|---|
| 影片 | AI 虚拟人(数字人口播,照片+文案→对口型视频) | `video`(wan2.2-s2v) |
| 影片 | 文字转影片 | `video_t2v` |
| 影片 | 图片转影片 | `video_i2v` |
| 影片 | 参考生成影片(多模态参考:图+视频+音频) | `video_r2v` |
| 影片 | AI 影片编辑器(换装/局部替换…) | `video_edit` |
| 图片 | AI 图片(文生图)/ AI 图片编辑器(图生图) | `ai_image` |
| 音频 | 文字转语音(多音色 / 克隆 / 设计音色) | `tts` |
| 音频 | AI 音乐 | `ai_music` |

### 平台能力(后端模块)

- **账户与多租户**:平台超管(/admin)→ 机构(租户)→ 成员;租户间数据隔离;RBAC(管理员/创作者)。
- **能力网关**:统一调用 + 多 provider(百炼 / 火山方舟)+ key 加密 + DB 队列 + N 槽并发 worker 池 + 失败隔离。
- **形象库**:预置形象(平台自产中式形象)+ 自定义(照片/视频)+ 授权存证。
- **音色库**:预置人声 + 声音克隆(VC)+ 设计音色(描述生成)+ 语言/性别筛选。
- **作品/素材库**:草稿+成品管理、筛选、下载、重新编辑/生成。
- **积分计费**:reserve(提交预扣)→ settle(成功结算)→ release(失败退还);按真实秒/字/张 × 倍率计价;充值订单 + 发票;套餐价格支持到分(最小 0.01 元);「申请扩容」生成销售意向线索(超管「意向线索」页可见机构与发起人)。
- **在线支付**(v0.6.0.0):微信支付(APIv3 公钥模式,PC 扫码 / H5)+ 支付宝(RSA2,电脑网站 / 手机网站)+ 对公转账;通道抽象层(`src/payments/`),商户号/密钥超管后台加密配置、场景级启停;支付成功积分自动入账;**原路退款**(积分自动扣回);**每日自动对账** + 差异面板(漏单/金额不符/通道侧退款);丢回调兜底(服务端 60s 主动查单 + 前端穿插查询)。运维见 `docs/PAYMENTS-RUNBOOK.md`,设计见 `docs/designs/online-payments.md`。
- **合规**:AI 生成标识(ffmpeg 后处理)、内容送审钩子、操作审计、滑块验证。
- **私有化**:Docker Compose 一套交付到客户内网。

---

## 2. 路线图(P1/P2 待办)

> P0 核心已上线(见 §1)。以下为后续增强,详见 `TODOS.md`。

| 模块 | P1 二期 | P2 三期 |
|---|---|---|
| 账户 | SSO / LDAP 对接、审计增强 | — |
| 形象 | 形象编辑增强、高精定制训练(C4,高风险见 §4) | — |
| 音色 | 在线录制克隆、批量管理 | — |
| 影片 | 多场景模板、自动字幕、批量生成 | — |
| 素材 | 智能分析(自动打标签、以图搜图) | — |
| 作品 | 协作、分享链接、标签体系 | — |
| 计费 | 套餐增强 | — |
| 合规 | 人工复审流 | — |
| 音频 | — | 歌声迁移(SVC 翻唱 + 歌词校正) |

---

## 3. 合规(融媒体硬要求,已落地基础)

- **AI 生成标识**:成片按监管要求加标识(ffmpeg 后处理 / 元数据)。
- **送审**:文本/形象/音频/成品送审钩子,拦截违规。
- **授权存证**:克隆声音/上传形象须本人授权确认。
- **审计**:谁、何时、生成了什么(政企合规)。

---

## 4. 攻坚点 / 风险

### 4.1 高精形象定制训练(C4,P1,高风险)
- ⚠️ **成本**:云端定制训练按"分身"计费,单价高。
- ⚠️ **周期**:训练长时(数小时),部分厂商需人工审核(数天)。
- ⚠️ **依赖**:需确认百炼/其它厂商提供"基于视频的高精数字人定制训练"API 及 SLA/价格/审核流程。
- 落地建议:UI 与任务流程先做完整(发起→排队→训练中→完成入库),支持"审核中"中间态。

### 4.2 歌声迁移(P2)
- ⚠️ 云端 SVC 成熟方案少、音质调优耗时。三期前需锁定可用的云端 SVC 厂商或外采方案。

### 4.3 私有化下的公网素材 URL(T-PUBLIC-URL)
- wan2.2-s2v 要素材公网可达;客户内网下需中转方案(见 `TODOS.md`)。

---

## 5. 验收基线(可测,已达成)

1. 上传 1 张合规照片 → ≤2 分钟得到可用自定义形象。
2. 上传 5–30s 音频样本 → 成功克隆音色并试听。
3. 选形象+音色+≤2000 字文案 → 生成口型自然的数字人视频(16:9 / 9:16)。
4. 生成前显示费用预估;成功后按真实秒数正确扣减积分,消费记录可查。
5. 单任务失败显示"失败"且可重试,**不影响其它任务和平台可用性**(失败隔离)。
6. 成片带 AI 生成标识(符合监管)。
7. 同一套前端,切换托管/私有化配置功能一致(网关可换账号)。
8. 角色权限隔离:创作者不能管理成员;跨租户不可见彼此数据。

---

## 6. 与短剧项目(quying)的关系

本平台独立于 `~/dev/workspace/ai/waoowaoo`(quying 短剧项目)。技术栈互相借鉴 provider 网关、
队列编排、资产中心等模式,但**独立仓库、独立部署、运行时隔离**,互不牵连。
