# 灵镜 · 融媒体数字人 SaaS 平台 — 功能清单 / PRD

> 版本 v0.1 · 2026-06-01 · 状态:规划中(待评审)
> 本文档是产品功能规格,作为后续开发、报价、私有化交付的依据。
> 原型见 `prototype/`;原始需求见 `~/Downloads/数字人原需求及硬件配置.xlsx`。

---

## 0. 产品定位与约束

| 项 | 内容 |
|---|---|
| 产品 | 灵镜 · 数字人内容生产 SaaS 平台 |
| 目标客户 | 融媒体机构(电视台、报社、广电、政企宣传部门) |
| 核心价值 | 上传照片/视频 → 输入文案 → 云端生成口型自然的数字人讲话视频;声音克隆配音 |
| 技术路线 | 能力全部走**云端 API**(以阿里百炼为主),不自建 GPU |
| 交付形态 | **双模式**:①托管(开账号、按量计费);②私有化(整套部署到客户处) |
| 界面形态 | 左侧功能模块导航的"系统"形态 |

### 0.1 关键架构原则:能力网关层(贯穿全平台)

所有云端能力调用必须经过**统一能力网关层**抽象。业务与 UI 只声明"生成数字人视频""克隆音色",不直接绑定厂商。

- 托管模式:网关后接**我们的云账号**(百炼),计费走我们的账户,向客户按量透传收费。
- 私有化模式:网关后可换成**客户自己的云账号 key**,或客户指定的其它厂商。
- 收益:**一套代码,两种交付**;某厂商额度耗尽/故障时可切换(规避"免费额度耗尽""模型配错全站不可用"这类风险)。

---

## 1. 已锁定的决策

| 决策点 | 结论 |
|---|---|
| MVP(一期)范围 | 数字人视频 + 声音克隆 + **高精形象定制训练** + 用户/计费 |
| 歌声迁移 | **三期/后置**(云端 SVC 方案不成熟,音质调优耗时) |
| 高精形象定制训练 | **一期必须有**(⚠️ 重活,见 §4.3 风险标注) |
| YOYA 多模态素材 | **自研基础素材库**;智能分析(自动打标签)二期增强,不对接外部 YOYA |

---

## 2. 优先级与分期总览

优先级:**P0** 一期必做(MVP) · **P1** 二期 · **P2** 三期。

| # | 模块 | P0 一期 | P1 二期 | P2 三期 |
|---|---|---|---|---|
| A | 账户与机构(用户/权限/席位) | ✅ 基础 | 增强(审计/SSO) | — |
| B | 能力网关层 | ✅ | 多厂商路由 | — |
| C | 形象库(预置/自定义/高精训练) | ✅ 全部 | 形象编辑增强 | — |
| D | 音色库 + 声音克隆 | ✅ | 在线录制/批量 | — |
| E | 数字人视频创作 | ✅ | 多场景/字幕/批量 | — |
| F | 作品库(草稿+成品) | ✅ | 协作/分享/标签 | — |
| G | 素材库(基础管理) | ✅ 基础 | 智能分析(打标签) | — |
| H | 用量与计费 | ✅ | 套餐/发票 | — |
| I | 内容审核与 AI 标识 | ✅ 基础 | 人工复审流 | — |
| J | 声音迁移(语速/音量/合成管理) | ✅ 随 D | — | — |
| K | 歌声迁移(翻唱+歌词校正) | — | — | ✅ |
| L | 私有化部署能力 | ✅ 架构预留 | 完整交付包 | — |

---

## 3. MVP(P0)功能清单 — 详细

### A. 账户与机构

> 采用假设(待你确认):多租户 SaaS,机构 → 成员 → 角色三级。

- A1 机构(租户)注册/开通;一个机构 = 一个隔离的资产/作品空间
- A2 成员管理:邀请成员、停用、移除
- A3 角色与权限(基础三角色):
  - **管理员**:成员管理、用量查看、配置
  - **创作者**:形象/音色/素材/作品的创建与使用
  - **查看者**:只读作品、下载
- A4 席位概念:机构购买 N 个创作席位(托管模式计费维度之一)
- A5 登录认证:账号密码 + 短信/邮箱验证(私有化可对接客户 LDAP/SSO,P1)

### B. 能力网关层(后端核心,无独立 UI)

- B1 统一调用接口:数字人视频、TTS、声音克隆、(后期)歌声迁移、多模态分析
- B2 厂商凭证配置:托管=平台账号;私有化=客户账号(管理后台可配)
- B3 任务队列 + 异步编排:所有生成任务进队列,失败重试,worker 异常隔离(单任务失败不影响全站)
- B4 实时进度推送(SSE):前端可见排队位次、进度、预计时间
- B5 计量埋点:每次调用记录 token/时长/成本,供计费透传

### C. 形象库

- C1 **预置形象**:不少于 2 个(满足原始需求),建议起步 6–12 个不同性别/风格主播
- C2 **自定义形象 — 照片**:上传 jpg/png/webp(≤30MB),即时生成可用形象
  - 上传校验提示:闭嘴中性表情、正脸看镜头、无遮挡(见原型"创建角色"弹窗)
- C3 **自定义形象 — 视频**:上传 mp4 提取形象
- C4 **高精形象定制训练(P0,⚠️重)**:基于客户高清视频训练 2 款专属高精分身
  - 训练任务异步、长时(云端可能数小时);训练状态可见(训练中/完成/失败)
  - 完成后进入"我的形象 — 定制"分类,可选用创作
  - 见 §4.3 风险与依赖
- C5 形象分类浏览:预置 / 我的 / 定制训练;搜索、筛选(性别、场景、横竖比)
- C6 形象管理:重命名、删除、设为默认

### D. 音色库 + 声音克隆

- D1 **预置人声**:40 个(满足原始需求,含 400 小时语音包概念),带语言/性别/适用场景标签、试听
- D2 **声音克隆**:上传 MP3/WAV(5–30s,≤100M)→ 复刻专属音色
- D3 个人音色管理:试听、重命名、删除;集中列表
- D4 音色筛选:语言、性别(见原型"所有声音"侧栏)
- D5 克隆状态可见(复刻中/完成/失败)
- D6 在线录制音频克隆(P1,需浏览器录音 + 文案提示)

### E. 数字人视频创作(核心流程)

创作流程(向导式,见原型 `create.html` 左配置 + 右对话流):

- E1 选形象(从形象库选,或当场新建)
- E2 选音色(从音色库选预置/克隆,或当场克隆)
- E3 输入文案:**≤2000 字**(满足原始需求),实时字数;或**上传本地音频(mp3/wav)驱动**生成
- E4 配音参数:**语速 0.5–2 倍、音量可调**(满足模块3需求,与声音迁移共用能力)
- E5 视频参数:分辨率(1080P 等)、比例(16:9 / 9:16 / 1:1)、背景
- E6 **自定义视频背景**:上传高清背景(满足原始需求)
- E7 **生成前费用预估**:基于时长/分辨率展示预估点数(计费透传核心,见原型)
- E8 提交生成 → 进队列 → 右侧对话流实时展示三态:**生成中 / 已完成 / 失败可重试**
- E9 成品操作:播放、下载、重新编辑、重新生成、删除

### F. 作品库

- F1 统一管理草稿 + 成品(满足原始需求"各阶段草稿和音视频结果")
- F2 列表/网格视图;按类型(视频/音频)、状态、时间筛选
- F3 搜索、删除、下载、批量操作(满足模块3"批量"需求)
- F4 草稿继续编辑;成品分享链接(P1)

### G. 素材库(基础)

> YOYA = 自研基础素材库(已确认),智能分析放 P1。

- G1 素材上传:图片/视频/音频
- G2 分类、文件夹、搜索、重命名、删除
- G3 与形象/音色/作品打通(素材可直接用于创作)
- G4 (P1)**智能分析**:云端多模态模型自动打标签、内容理解、以图搜图

### H. 用量与计费

> SaaS 命脉。托管模式必须第一天就有。采用假设:点数(credits)制。

- H1 点数余额展示(顶栏,见原型"剩余 1,280")
- H2 用量计量:每次生成扣点,按功能(视频/克隆/训练)区分单价
- H3 消费记录:时间、功能、消耗点数、关联作品,可查询/导出
- H4 充值/套餐(托管);私有化模式可关闭计费或改为内部统计
- H5 用量预警:余额不足提示(避免出现"额度耗尽"突然不可用的体验)

### I. 内容审核与 AI 生成标识(合规,融媒体硬要求)

- I1 **AI 生成内容标识**:生成视频/音频按监管要求添加标识(显式水印/元数据)
- I2 文本/形象/音频送审:接入审核能力,拦截违规内容
- I3 操作审计日志:谁、何时、生成了什么(政企合规)
- I4 (P1)人工复审流:可疑内容转人工

### L. 私有化部署能力(架构预留)

- L1 一期架构上保证可私有化:能力网关可换客户云账号、数据可本地化
- L2 (P1)完整私有化交付包:部署文档、配置后台、离线安装

---

## 4. 二期 / 三期 与攻坚点

### 4.1 二期(P1)要点
- 声音在线录制、批量音色管理
- 数字人视频:多场景模板、自动字幕、批量生成
- 素材智能分析(自动打标签/检索)
- 作品协作、分享、标签体系
- 计费:套餐、发票;账户:SSO/审计增强
- 私有化完整交付包

### 4.2 三期(P2):歌声迁移(K)
满足模块3第 7、9–11 条:
- 上传音乐 / 手机扫码上传(wav/mp3/m4a ≤10MB,10s–5min)
- 用克隆音色迁移演唱声(SVC)
- 自动识别歌词 + 大模型校正歌词
- 迁移歌曲的播放/试听/下载/删除/查看歌词
- ⚠️ **攻坚点**:云端歌声转换(SVC)成熟方案少、音质调优耗时。需求侧:三期前需确认可用的云端 SVC 厂商或外采方案。

### 4.3 高精形象定制训练(C4,一期但高风险)
- ⚠️ **成本**:云端定制训练通常按"分身"计费,单价高;调用量放大需核算
- ⚠️ **周期**:训练任务长时(数小时),且部分厂商需人工审核(数天)
- ⚠️ **依赖**:需确认百炼或其它厂商提供"基于视频的高精数字人定制训练"API 及其 SLA/价格/审核流程
- 一期落地建议:UI 与任务流程先做完整(发起训练→排队→训练中→完成入库),能力对接以选定厂商为准;若厂商审核需人工介入,UI 需支持"审核中"中间态

---

## 5. 验收标准(MVP 关键项,可测)

1. 创作者上传 1 张合规照片,可在 ≤2 分钟内得到一个可用自定义形象
2. 上传 12s 音频样本,可成功克隆音色并试听
3. 选形象+音色+输入≤2000字文案,提交后生成口型自然的数字人视频,16:9/9:16 均可
4. 生成前显示费用预估;生成后正确扣减对应点数,消费记录可查
5. 生成任务失败时,作品库/对话流显示"失败"且可重试,**不影响其它任务和平台可用性**
6. 生成视频带 AI 生成标识(符合监管)
7. 同一套前端,切换托管/私有化配置后功能一致(能力网关可换账号)
8. 三角色权限隔离:查看者不能发起生成,创作者不能管理成员

---

## 6. 待你最终确认的开放项

1. **用户/权限模型**:机构→成员→三角色(管理员/创作者/查看者)+ 席位 —— 是否符合你的销售模式?
2. **计费单位**:点数(credits)制 —— 还是按视频条数/包月套餐?私有化模式计费是否完全关闭?
3. **合规标识**:AI 生成标识的具体形式(水印样式、是否可关)需按目标客户所在地监管细则定
4. **高精训练厂商**:百炼是否提供"基于视频的高精数字人定制训练"?价格/周期/审核如何?(决定 C4 一期能否如期)
5. **歌声迁移厂商**:三期前需锁定可用的云端 SVC 方案
6. **品牌名**:暂用「灵镜」,是否定稿?

---

## 7. 与短剧项目(quying)的关系
本平台独立于 `~/dev/workspace/ai/waoowaoo`(quying 短剧项目)。技术栈可借鉴 quying 的 provider 网关、队列编排、资产中心、SSE 进度模式,但**独立仓库、独立部署、运行时隔离**,互不牵连。
