# macOS
.DS_Store

# 编辑器
.vscode/
.idea/
*.swp

# 密钥 / 环境变量（绝不提交）
.env
.env.local
*.local

# 运行时数据库（本地/部署生成,绝不提交）
*.db
*.db-shm
*.db-wal

# 依赖 / 构建产物（将来工程化时用）
node_modules/
dist/
.next/
build/
.gstack/
probe-output.mp4
probe-labeled.mp4
.gitnexus

# 探索/落地页样例封面:真图存 OSS 公网,本地 jpg 不入库(生产由 OSS/CDN 伺服)
prototype/showcase/*.jpg
prototype/showcase/probe-*.jpg
prototype/showcase/*.mp4
prototype/showcase/*.wav
