# Changelog

灵镜平台的用户可见变更记录。版本号为四段式 `MAJOR.MINOR.PATCH.MICRO`(与 `VERSION` 文件一致)。

## [0.9.2.0] - 2026-07-28

### Added
- **MCP 补齐到平台创作能力全集**。此前接入 Claude Code / Cursor 的 Agent 只能做文生图、图生图、文字转影片和配音 —— 平台八类创作能力里,**图片转影片、参考生成影片、影片编辑三类在 Agent 侧根本不存在**,而 AI 音乐虽然列在工具箱里却必然报错。现在新增 `generate_video_from_image`(首帧 / 首尾帧 / 参考生成三种用法)、`generate_video_from_refs`(图 + 视频 + 音频任意组合)、`edit_video`(换装 / 风格化 / 局部替换),配套 `upload_video` 与 `upload_audio` 两个上传口,三条闭环全程不用离开 MCP。
- **提交前可以先问价:任意生成工具传 `dry_run: true`**,只返回预估费用与当前余额,不提交、不扣积分。它走的是与真提交**完全相同**的校验,所以「问价通过」就意味着「同参数提交也会通过」——不会再出现报价给了数字、提交却被拒的情况。
- **超时重试不再重复扣费**:生成工具新增可选的 `idempotency_key`,重试同一次请求传相同值即可拿回原任务;想再要一份就换个新值。此前 MCP 侧完全没有幂等保护,而 Agent 超时重试是常态,视频这类分钟级任务重试一次就是双倍花费。
- **Agent 可以自查了**:新增 `get_balance`(查积分余额)与 `list_jobs`(列最近任务)。此前余额只能等提交被拒才知道,会话中断后丢失的 `job_id` 也再找不回来。
- **文字转语音补齐情绪、语速、音高、语言四个参数**,与网页端能力对齐。
- **连上 MCP 即获得一份使用说明**:异步轮询节奏、先发现再生成、合规红线,以及**哪些能力只能在后台网页端完成**(数字人口播、创建形象、声音克隆)。此前这份说明是空的,Agent 只能靠猜,常常反复尝试根本不存在的功能。
- 开放 API 文档(`/api-docs.html`)重写 MCP 章节:16 个工具与 REST `type` 的对应表、三条闭环、`dry_run` 与幂等用法、大文件改走 REST 的示例;错误码表补全并标注 REST / MCP 适用范围。README 新增开放接口一节。

### Fixed
- **AI 音乐在 MCP 里从「必然失败」变成可用**。`generate_music` 的参数表里缺了必填的 `mode`,而服务端要求它 —— 更麻烦的是多传一个 `mode` 也会被参数校验丢弃,Agent 无论怎么试都过不去,只能一路重试到限速。现补齐 `mode`(有人声 / 纯音乐)、歌词、人声性别与模型选择。
- **报价与实扣的口径统一**。旧的费用预估接口对某些参数比真提交宽松:时长超出模型范围会被悄悄截断后照样给价、分辨率不合法也照样计价、音色是否存在根本不检查。Agent 拿着这样一个报价去提交就会被拒,而它对这类错误无法自救。MCP 侧的问价现已改走与提交完全相同的校验路径。(网页端的实时报价维持原样 —— 它需要在参数填一半时也能出个数。)
- **模型与音色列表不再暴露内部信息**。MCP 的发现类工具此前把内部对象原样返回,其中包含上游厂商的真实模型编号与每秒成本档位;网页端一直是过滤后下发的。现在两边共用同一份字段投影,音色列表也不再返回样本存储路径等内部字段。
- **上传超时重试不再产生重复的授权存证**。上传的素材改为按**内容指纹**命名:同一个文件重传落回同一个位置,不再生成孤儿文件,合规存证表里也不会为一次授权留下两条记录。授权存证新增素材归属字段 —— 此前存证行与它所证明的那个素材之间没有任何关联,审计时答不出「这张图的授权是哪一条」。
- MCP 请求体超限的提示不再只说「减少图片张数」——那对视频上传是错误的建议;现按实际情况提示,并给出改走 REST 上传端点的具体路径。
- MCP 握手回给客户端的版本号改为读取实际发布版本,不再是写死的旧值(客户排查问题时对不上号)。

### Security
- **新增上传通道的并发数据量上限**。此前的限速只管「每分钟多少次请求」,管不住「同时有多少数据在服务端内存里」;新增两条 20MB 上传路径后,十几路并发上传就能造成数百 MB 的瞬时占用。现按请求声明的大小在读取请求体**之前**记账,超过总量直接返回可退避的 429。私有化部署可用 `MCP_INFLIGHT_BYTES` 调整(默认 128MB),填了非法值会告警并回落默认。

### Changed
- **参考音频上传现在也要求授权确认**(`POST /api/audio-uploads` 与 MCP 的 `upload_audio` 都需 `consent`)。此前只有图片和视频要求,音频按「参考素材」走轻量口径 —— 但参考音频可以直接驱动影片生成,一段真人录音在没有任何授权声明的情况下就能变成成片,而声纹同样属于个人信息。网页端的授权勾选文案本来就写着「图片/视频/音频」,只是后端此前没有真正校验;现在两个入口口径一致,并与图片/视频一样写入授权存证。**这对不传 `consent` 的旧 API 调用方是破坏性变更**。

### Deprecated
- **MCP 的 `estimate` 工具已标记废弃**,请改用各生成工具的 `dry_run: true`。它仍可调用(且已改走严格校验),但参数是扁平化的,覆盖不了图转影片 / 参考生成 / 影片编辑的完整字段。计划在后续版本移除。

## [0.9.1.1] - 2026-07-26

### Added
- **MCP 新增 `upload_image`:图生图在 Agent 里终于跑得通了**。此前 `generate_image` 收 `imageRefs` 参数,但 MCP 工具集里没有任何工具能产出它 —— 接入 Claude Code / Codex 的 Agent 想做图生图,在工具箱里找不到上传口,只能去猜接口路径,最后误报成「API 密钥权限不足」。现在闭环是:`upload_image` 传图拿 `imageRefs` → `generate_image` 带 `mode="img2img"` 提交 → `get_job` 取结果,全程不用离开 MCP。图片用 base64 内联,单张 ≤10MB、一次 ≤9 张,单次请求体上限 32MB。
- 合规口径不变:含真人的参考图仍必须 `consent=true` 并写授权存证,与网页端、REST 共用同一套校验 —— 不因为是 Agent 调用就放松。

### Fixed
- **403 错误不再把人引向错误结论**。API 密钥的作用域守卫挂在路由之前,所以**路径写错(接口根本不存在)也会返回 403**,与真的权限不足长得一模一样。此前的文案只说「无权访问」,导致调用方把「路径拼错」当成「密钥少了权限」,回头去找管理员加权限。现在 403 会明确提示这一点,并在 `allowed` 字段里直接列出全部可用端点。
- **MCP 请求体超限不再返回 HTML 错误页**。超过上限时返回结构化的 `413 PAYLOAD_TOO_LARGE`,并给出「减少张数或压缩图片」的可执行建议。
- 上传文件名里的异常字符(超长、路径分隔符等)不再进入素材的存储路径,避免个别文件上传后无法访问。

### Security
- **未携带有效密钥的 MCP 请求,在服务端读取请求体之前就被拒绝**。图生图要传图,MCP 的请求体上限必须放宽;若不同时把鉴权前移,匿名请求就能让服务端先吃下一大块内存再被拒,成为廉价的资源消耗手段。鉴权与粗粒度限速现已前置到读取请求体之前。私有化部署可用 `MCP_BODY_LIMIT` 调整上限(默认 32MB);填了非法值会告警并回落默认,不会静默关闸。

## [0.9.1.0] - 2026-07-26

### Added
- **「成员用量」板块**(用量计费页,管理员可见):按成员看生成次数、消耗点数与工具分布,支持今日 / 本月 / 近 30 天 / 全部切换。上一版把生成内容彻底私有化后,管理员需要一个能回答「谁在烧钱、烧在哪」的入口 —— 这里**只呈现数字,不含任何提示词或成品**,机构管好预算的同时不触碰成员的创作内容。
- **成员与权限页新增「本月消耗」列**(管理员可见),点数字直达上述板块。

## [0.9.0.0] - 2026-07-25

### Changed
- **生成内容彻底私有:管理员也看不到他人的作品与资产**。此前机构管理员可以查看、下载、删除本机构所有成员生成的图片/视频/音频与生成记录,也能看到他人的自定义形象与克隆音色。现在这些一律**按人隔离**:每个人只能看到、下载、重试、删除自己创建的内容,机构管理员与平台超管都不例外。作品库、我的资产、各创作工具的生成记录,以及形象库、音色库全部生效;网页端与 Open API / MCP 同一口径,不能从接口侧绕过。
- **不能拿他人的形象或音色去生成**:自定义形象与克隆音色仅创建者本人可用(预置形象/音色仍全员共享)。
- **「设为默认形象」改为按人生效**:此前是机构内单例,一个人设默认会清掉别人的默认;现在每人各有自己的默认形象,互不影响。
- **用量计费改为「只看账不看内容」**:机构管理员仍能看到每个成员花了多少积分、用了哪个工具、什么时候花的(机构要管预算),但**看不到提示词摘要、也点不开成品预览**。成员本人查看自己的消费明细不受影响。
- 平台超管控制台维持原样:只看运营元数据(租户 / 模块 / 模型 / 积分 / 状态 / 耗时 / 失败原因),本就不含提示词与产物,现新增回归测试锁死,防止后续改动把生成内容带进后台。
- 充值订单、发票与审计日志的可见性**不变**(财务与合规口径,机构管理员仍可查看)。

### Note
- **历史数据不迁移**:本次改动前产生的、没有归属记录的作品与资产(数据库中 `created_by` 为空),改动后对所有人不可见,包括管理员。这是刻意取舍——按用户决策,老数据不做迁移。已有归属记录的数据不受影响,原创作者照常可见。

## [0.8.0.6] - 2026-07-25

### Fixed
- 无用户可见变更。本次只修测试基建:全量测试此前约 20% 概率随机某个文件整体失败(端口资源竞争,非真实缺陷),会让「测试全绿」这个发布闸门失去意义。现已修复,并加了护栏防止同类问题再次混入。

## [0.8.0.5] - 2026-07-25

### Added
- **生成记录标注来源**:Agent 通过 MCP 或 API 密钥提交的作品,在记录卡上显示「MCP 创建」/「API 创建」徽章 —— 一眼分得清哪些是自己在网页上点的、哪些是接入的 Agent 跑的。各创作工具的记录流、「我的资产」都能看到。自己在网页上创建的作品不显示徽章(不打扰)。此前上线前创建的老作品同样不显示徽章。

## [0.8.0.4] - 2026-07-23

### Changed
- 顶栏「API 密钥」入口从纯图标改为图标+文字,辨识度更高(窄屏回落纯图标)。

## [0.8.0.3] - 2026-07-23

### Changed
- **密钥可随时复制**:API 密钥不再"只在创建时显示一次",列表每行新增「复制」按钮,随时取回完整密钥。密钥以 AES-256-GCM 加密存储(需 `MASTER_KEY`,与第三方厂商密钥同一套),数据库单独泄露拿不到密钥;只能复制自己的明文(管理员也不能复制他人明文)。每次复制写审计。
- 说明:此前创建的旧密钥无加密副本,无法复制(提示删除重建);正式部署后新建的密钥均可随时复制。

## [0.8.0.2] - 2026-07-23

### Changed
- **API 密钥入口移到顶栏**:右上角积分左侧新增钥匙图标,一键进入 API 密钥页(全站可达)。
- **创建密钥改为弹框**:点「＋ 创建密钥」弹框填名,创建成功后密钥明文带**复制密钥**按钮(一次性),接入命令自动填好。
- **密钥可禁用 / 删除**:「禁用」停用但保留记录(可审计),「删除」永久移除;原「吊销」措辞统一为「禁用」。
- 移除页面上的 CLI「即将上线」标识(Agent 接入走 MCP,无需 CLI)。

## [0.8.0.1] - 2026-07-23

### Changed
- **API 密钥独立成页**:从系统设置迁到独立的「API 密钥」页(右上角头像菜单进入),密钥管理与开发者接入合并到一处。
- **Agent 一键接入**:密钥页给出三种接入方式——① `curl .../connect.sh | sh -s -- <mcp_url> <key>` 一键脚本(自动识别 Claude Code 并配好 MCP);② 等价 `claude mcp add` 原生命令;③ 通用 MCP 配置 JSON(OpenClaw / Cursor / Codex 等复制即用)。创建密钥后命令自动填好;换机器可用「粘贴密钥生成命令」(密钥不上传服务器)。
- 明确 Agent 接入走 **MCP 协议**(原生工具调用),REST 为底座。

## [0.8.0.0] - 2026-07-23

### Added
- **MCP 接入(Claude Code / Cursor 原生调用)**:在 Agent 里加一段配置(端点 `/mcp` + `Authorization: Bearer lj_sk_…`),即可用原生工具 `generate_image` / `generate_video` / `generate_music` / `generate_speech` / `get_job` / `list_models` / `list_voices` / `list_avatars` / `estimate` 调用灵镜。生成类工具异步返回任务 id,用 `get_job` 轮询取签名下载 URL。认证、限速、计费与开放 API 完全一致。

## [0.7.0.0] - 2026-07-23

### Added
- **开放 API(API 密钥)**:让机构的 AI Agent 通过 HTTP 直接调用生图 / 生视频 / 生音乐 / 语音。成员在系统设置自助创建密钥(`lj_sk_…`,明文仅创建时显示一次),密钥即本人身份、消耗本机构积分。管理员可查看并吊销全租户任意密钥。
- **接入文档页** `/api-docs.html`:认证、提交生成、幂等、查询/下载、模型列表、上传与合规、限速、错误码表。
- **幂等键**:提交生成支持 `Idempotency-Key` 请求头,Agent 超时重试不会重复扣费(同键同请求返原任务,异请求体 409)。
- **审计标记**:经 API 密钥发起的操作在审计日志标记来源密钥,区分本人网页操作与 Agent 操作。

### Security
- API 密钥作用域受限:只能访问生成相关接口,越界访问账号 / 成员 / 支付返回 403;读写分级限速防滥用;主人停用或密钥吊销即时失效。
- 开放接口错误统一携带机器可读 `code`(`UNAUTHORIZED` / `SCOPE_FORBIDDEN` / `ROLE_FORBIDDEN` / `RATE_LIMITED` / `INSUFFICIENT_CREDITS` / `IDEMPOTENCY_CONFLICT`),便于 Agent 判断是否重试。

## [0.6.0.1] - 2026-07-23

### Security
- **修复输入素材跨租户越权(IDOR)**:此前提交生成任务时,登录用户可携带其他机构的输入素材 key(图/视频/音频引用)进行图生图、图生视频、参考生视频、视频编辑,读取到不属于本机构的素材并绕过深度合成授权闸。现在提交与费用预估入口统一校验输入素材必须属于本机构,越权或伪造引用一律拒绝。数字人形象/音色不受影响(本就按账号校验)。

## [0.6.0.0] - 2026-07-14

### Added
- **在线支付:充值可用微信支付 / 支付宝,秒级到账**。收银台新增付款方式选择:微信(PC 扫码 / 手机浏览器)、支付宝(电脑网站 / 手机网站)、对公转账。支付成功后积分自动入账,页面自动变绿,用户无需任何点击
- 收银台体验:二维码倒计时 + 过期一键刷新、支付方式可随时切换、支付方式记忆、手机支付返回自动检测结果、微信内打开时长按识别二维码引导、支付成功积分跳动动效
- **超管「在线支付」页**:填商户号/密钥即开通(密钥加密存库,保存后不回显),微信/支付宝、扫码/H5 场景可分别启停;未配置的通道在收银台显示「敬请期待」,对公转账不受影响
- **原路退款**:超管可在订单管理对已到账的在线支付订单发起整单退款,退款完成后积分自动扣回(已开票订单需先驳回发票)
- **每日自动对账**:每天与微信/支付宝账单逐笔核对,漏单、金额不符、通道侧手工退款等差异自动落到超管「对账差异」面板,可一键原路退回或标记已处理
- 「申请扩容」按钮真实落库:用量计费页点击后生成意向线索,超管「意向线索」页可看到申请机构名称与发起人

### Changed
- 积分套餐价格支持到分(最小 0.01 元),便于 1 分钱联调与低价体验套餐
- 订单台账新增「退款中 / 已退款」状态与「秒到账」标识;超管订单管理新增「已退款」筛选
- 用量计费流水新增「退款扣回」类型徽章

### Fixed
- 支付回调若未送达(网络/配置问题),服务端每分钟主动向通道查单、前端约 9 秒穿插查询 —— 支付成功后最迟约 12 秒页面自动到账,不再需要手动点击查询
- 收银台不再对在线支付显示对公流程的「我已支付」按钮(在线支付本就自动到账,该按钮会误导用户以为必须点击)
- 已驳回的订单不再进入支付页(此前所有操作都是死路),直接引导重新下单
- 付款方式选择支持键盘操作;移动端触控目标补足到 44px

## [0.5.2.2] - 2026-07-08

### Fixed
- 部署:`docker-compose.yml` 的 app 服务未转发 `OPENAI_PROXY`(只有 `GEMINI_PROXY`)。导致 .env 里配了 `OPENAI_PROXY` 也进不了容器 → GPT Image 2 直连 `api.openai.com` 被墙 `fetch failed`。补 `OPENAI_PROXY: ${OPENAI_PROXY:-}`,与 Gemini 同机制

## [0.5.2.1] - 2026-07-08

### Fixed
- 各生成记录模块「加载更多」按钮错误显示在列表**顶部** → 修正为固定在列表**底部**(历史卡片插入到按钮之前);符合「首屏 20 条 + 点击加载更多向下续拉」的直觉

## [0.5.2.0] - 2026-07-08

### Fixed
- 各模块生成记录、我的资产、作品列表改为**服务端分页 + 按类型/来源过滤**(此前全局只取最新 50 条再前端过滤)。根治「其他工具刷屏后本模块记录为空 / 看不到更旧记录」的截断问题:每个模块只拉自己类型的记录,分页返回

### Added
- 生成记录 / 我的资产 / 作品:新增「加载更多」,可翻阅更早的历史记录(此前无法访问最新 50 条以外的作品)

## [0.5.1.0] - 2026-07-08

### Fixed
- 用量计费统计改为**整租户后端汇总**(新增 `/credits/summary`),不再从分页明细现算 —— 修复:①「近 30 天暂无消耗」误报(实有消耗);②「累计生成」严重偏少(只统计当页);③「累计发放」显示 0
- 「本月消耗」改为**真实净扣**(扣除失败任务退还),不再把失败任务的预扣计入

### Added
- 用量计费新增「今日消耗 / 今日生成次数」统计卡(整租户口径)

## [0.5.0.3] - 2026-07-08

### Fixed
- GPT Image 2 生成记录:显示所选**画质**(低/中/高);图片转图片/编辑不再显示无意义的「1K」清晰度(编辑输出恒标准尺寸);底部参数条同步显画质。文生图仍显清晰度 + 画质

## [0.5.0.2] - 2026-07-08

### Fixed
- GPT Image 2 内容被 OpenAI 安全审核拦截时,失败提示由误导性的「生成失败,请稍后重试」改为准确的「内容被 OpenAI 安全审核拦截,请修改提示词/输入图后再试(直接重试相同内容仍会被拦截)」;新增 OpenAI 组织未验证的可读提示

### Changed
- 图片编辑器 / 图片转图片:GPT Image 2 编辑输出为标准尺寸(无清晰度档),新增说明提示,避免误以为缺失功能

## [0.5.0.1] - 2026-07-07

### Changed
- AI 图片 / AI 图片编辑器:提示词字数上限 2000 → 5000(不按模型区分)。超出个别模型限制的,由厂商接口返回错误,在生成记录以中文提示

## [0.5.0.0] - 2026-07-07

### Added
- **GPT Image 2 图片编辑 / 图生图**(OpenAI `/images/edits`):上传 1-9 张底图 + 提示词做编辑/融合,可选画质(低/中/高);输出为标准尺寸
- GPT Image 2 编辑同按**真实用量计费**:输出图 token + **输入底图 token($8/1M)** + 输入文本 token($5/1M)全部精算入价,预扣按上限、生成后按实结算多退少不补

### Changed
- token 计价改为按类精算:输出图($30/1M)、输入图($8/1M)、输入文本($5/1M)按各自费率折算,商业自负盈亏(编辑输入底图成本不再由平台承担)
- 图片编辑器新增 GPT Image 2:显示画质选择器,分辨率随编辑端点固定(仅标准尺寸)
- 图片能力网关编辑接口(editImage)同步返回用量信息(供 token 结算);百炼/火山/Gemini 行为不变

## [0.4.0.0] - 2026-07-07

### Added
- AI 图片新增 **GPT Image 2**(OpenAI,文生图):支持选择 比例 + 分辨率(1K/2K/4K)+ **画质(低/中/高)**,任意比例分辨率组合出图
- GPT Image 2 按 **真实用量(token)计费**:提交按上限预扣、生成后按 OpenAI 实际返回用量结算,**多退少不补**(费用预估显示「预估≤ X 点」)
- 管理后台「厂商」页新增 OpenAI:录入 API key 即可启用;国内服务器可配 `OPENAI_PROXY` 走出网代理(同 Gemini)

### Changed
- 图片能力网关同步返回用量信息(token 计价模型据此结算);百炼/火山/Gemini 行为不变
- GPT Image 2 计价费率以「每 token」为单位:管理后台若误按「每张」录价会被自动忽略、回落官方文档费率,杜绝错配导致的超额扣费

## [0.3.0.0] - 2026-07-06

### Added
- 失败任务错误可读化:厂商原始错误(火山方舟 / 阿里百炼 / Google Gemini)自动翻译为用户可读中文,前端不再出现类似 `HTTP 400: {"code":"InputImageSensitiveContentDetected.PrivacyInformation",...}` 的英文 JSON —— 例如「输入的图片/视频可能包含真人肖像,请更换为非真人素材后重试」
- 管理后台「消耗流水」失败行显示用户可读原因,并可展开查看厂商原始技术日志(含错误码 / RequestId),便于排障对账

### Changed
- 任务失败原因(`job.error`)口径收敛为「用户可读中文」;原始技术日志改存新增的 `error_detail` 列(仅平台管理员的消耗流水可见,租户/用户接口不暴露)
- 首次升级自动回填历史失败任务:原始错误存入 `error_detail`,`error` 列就地翻译为中文

## [0.2.0.0] - 2026-07-05

### Added
- AI 图片:Nano Banana 模型按清晰度分档计价 —— Nano Banana 2(Gemini 3.1 Flash)512/1K/2K/4K 档分别 12/17/26/39 积分,Nano Banana Pro(Gemini 3 Pro)1K/2K 档 34 积分、4K 档 61 积分;选低档更便宜,费用预估与实际扣费逐档一致
- 管理后台「统一定价」:分档模型可逐档录价、启停、直接补建档位价;档位缺失时页面给出预警;删除模型时连带清理其分档价,不留残留旧价

### Changed
- 管理员禁用某清晰度档即为该档下架:前端不再可选,提交与报价一并拒绝(不会静默按低档价出售高档图);全部档位下架的模型自动从模型列表隐藏
- 未选择清晰度时,统一按 2K 档生成并计费(校验、生成、计费三层使用同一显式值)
- 费用预估与提交使用同一套校验:非法清晰度、未知模型、不支持的模式在预估阶段即报错;预估失败时价格显示「—」而非残留上一次的报价

### Fixed
- Gemini 图片模型售价与后台定价表、全局毛利倍率脱钩的问题(此前后台改价/改倍率对这类模型不生效,一直按代码内置价收费)
- 管理后台图片模型列表显示的价格与启用状态与实际收费口径不一致的问题
