# 生产生成失败排查：2026-09-15

## 排查范围与结论

只读检查 `223.4.248.234` 的生产容器、SQLite 任务原始错误、Clash 配置和厂商任务状态。未修改生产源码、代理节点、任务状态或积分，未发起收费生成请求。生产 app 健康，未在本次排查中重启。

截至 2026-09-15 14:00（北京时间），9 月 9 日以来共 66 条任务：成功 40 条，失败 26 条。失败原因如下，不能全部归为代理故障：

| 原因 | 条数 | 证据与处理建议 |
| --- | ---: | --- |
| 参考视频超过厂商时长限制 | 2 | 9 月 15 日 11:43/11:44，Seedance 2.0 多模态参考生成 HTTP 400 `InvalidParameter`，明确要求参考视频 ≤15.2 秒。未创建厂商任务。先裁剪到 ≤15 秒；提交前校验和清晰错误提示另行确认后实现。 |
| OSS 请求超过默认 60 秒 | 5 | 9 月 9 日 19:16 同批 Seedance 2.5 图生视频，均有厂商 task_id。只读查询火山显示全部 `succeeded` 且有成品 URL；OSS 中对应最终视频 key 全部不存在。结合 worker 路径，失败发生在生成成功后的 OSS 保存阶段。 |
| OpenAI 网络请求失败 | 8 | `fetch failed`，不是 HTTP 参数错误。图片任务有成功也有失败，通用“服务器访问不到”提示不能证明代理地址配置错误。 |
| 百炼限流 | 7 | 9 月 9 日 16:18 同批 Qwen 图片编辑 HTTP 429 `Requests rate limit exceeded`。延长超时无效；建议按厂商/模型限制并发并退避，需另行确认。 |
| Gemini 网络请求失败 | 1 | `fetch failed`。 |
| Gemini 生成达到旧超时 | 2 | 600000ms 后 worker 主动终止，即原图片预算 10 分钟。 |
| Gemini 厂商服务繁忙 | 1 | HTTP 503 `UNAVAILABLE`，厂商明确返回 high demand。 |

截图中的 `ali-oss Response timeout for 60000ms` 属于存储请求层，不是模型生成轮询超时。当前数据库中同类记录可复现上述分类，但未将截图的 17:35 行与某个具体任务 ID 强行匹配。

## OSS：已生成成功但保存失败

已核验的 5 个任务 ID：

- `0a27f0b0-65a7-4b4c-837d-750e40a2f62d`
- `3c9f6383-b6ef-4eb9-9d08-840604a8a92b`
- `15e8579f-1f52-43d4-86d1-ad1e7ff5001e`
- `56262e2a-aff8-4e46-aa2e-57656ee740b6`
- `c5fb4795-d7ff-4b2f-9594-78a29c10b197`

`makeOssBackend` 原客户端没有设置 `timeout`，SDK 默认 60 秒，与原始错误一致。[ali-oss 官方说明](https://github.com/ali-sdk/ali-oss#ossoptions)

多个任务同时完成并上传可能增加传输耗时，但缺少当时带宽/丢包监控，不能断言是哪一种网络瓶颈。放宽超时解决的是“60 秒提前截断”，不保证完全消除存储网络故障。

上述任务不宜直接重新生成：厂商已经成功，重复提交可能再次产生厂商费用。可以在确认权限、成品 URL 有效性及退款/结算状态后，仅恢复成品并维护任务与账务一致性；本次未执行恢复。

## 代理：地址正确，但节点链路仍不稳定

容器内 OpenAI/Gemini 均配置 `http://host.docker.internal:7890`，请求带显式 undici 代理 dispatcher。当前 Clash：OpenAI → `OPENAI-STABLE` → `JP-01`；Google → `PROXY` → `AUTO` → `TW-01`。

从生产容器逐次建立新代理客户端、无 API Key 查询模型端点，OpenAI 5 次中 4 次收到 HTTP 401、1 次失败；Google 5 次中 3 次收到 HTTP 403、2 次失败。HTTP 返回证明对应请求已到达端点；失败的 cause 为 `ECONNRESET`，发生在 TLS 建立之前。小样本只能证明当前有不稳定，不能当作长期生成错误率。

本次未切换节点。若确认继续修复代理，应先独立测试候选节点，再只修改 `/opt/clash/config.yaml`，不把 Clash 配置加入业务源码。

## 本 worktree 已实现的超时调整

| 配置 | 默认值 | 范围 |
| --- | ---: | --- |
| `IMAGE_TIMEOUT_MS` | 900000ms（15 分钟） | 同步文生图/图生图请求，以及异步图片生成/编辑轮询；图片代理响应头/响应体等待也对齐。 |
| `VIDEO_TIMEOUT_MS` | 1800000ms（30 分钟） | 数字人、文生视频、图生视频、视频编辑、多模态参考视频轮询。 |
| `OSS_TIMEOUT_MS` | 1800000ms（30 分钟） | OSS 客户端上传/下载操作，独立于生成预算。 |

Compose 已转发三个变量。保留旧 `POLL_TIMEOUT_MS` 和 `VIDEO_T2V_TIMEOUT_MS` 的图片/视频配置回落，新变量优先。音乐等其它任务维持原通用超时；微信/支付宝代理维持原配置。

这些是生成请求/轮询及存储操作的分别预算，不是“含排队、审核、后处理、保存的端到端总任务硬上限”。未增加生成重试、未改变提交/预扣/退款/结算逻辑，也未自动裁剪用户素材。

## 验证与部署边界

- TypeScript 构建通过，Compose 默认插值验证为 900000/1800000/1800000。
- 新增 15 个测试：配置默认/覆盖/旧变量兼容、OSS 客户端超时、4 条图片路径、5 条视频路径、图片代理与支付代理隔离。
- 相关 15 个测试文件逐文件独立进程执行，105 个测试通过。
- 常规全量测试及限制并发后的全量测试遇到本机 Node 24 / better-sqlite3 原生清理钩子崩溃（`RemoveEnvironmentCleanupHook`）。逐文件独立进程全量执行结果：108 个文件中 81 个通过（1100 个测试通过）、27 个未通过，运行中仍出现相同原生崩溃，不能宣称全量验证通过。相关文件此前独立执行的 105 个测试已有通过结果，但部分再次执行受原生崩溃影响，测试环境需要复验。
- 经用户确认后，在独立 Linux Node 22.23.2 容器中使用原始已跟踪锁文件执行 `npm ci`、TypeScript 构建及全量测试。接入独立测试 MinIO 后，108 个文件通过，1396 个测试通过；6 个真实 FFmpeg 测试因容器暂未安装 FFmpeg 按原测试规则跳过。未连接生产存储、未修改代码规避原生崩溃。
- 补齐 FFmpeg 后再次全量执行：108 个文件通过，1401 个测试通过，仅 1 个与 FFmpeg 已安装条件互斥的降级测试跳过，退出码为 0。
- GitNexus 影响分析与变更检测已执行。共享 worker/图片代理涉及广泛流程，图谱风险为 HIGH/CRITICAL；实际变更仅限超时参数及配置，支付分支通过独立测试保持不变。
- 用户已有 `package-lock.json` 变更保持原样，不纳入本次业务修改。

用户已确认部署，生产生效状态以部署后核验为准。从本 worktree 的变更进行正常合并/部署，不直接编辑服务器源码。部署时显式设置新变量为上表值，以免宿主机旧超时变量覆盖默认值；上线前检查在飞任务，备份并保留可回滚镜像，只重建 app 后核验容器变量与健康状态，不主动发起收费生成请求。

已保留旧镜像 `lingjing-app:pre-timeouts-20260915`、旧源码及环境配置（`/root/lingjing-deploy-backups/20260915-timeouts/`），SQLite 使用在线 backup API 备份至数据卷中的 `/data/deploy-backups/20260915-timeouts/lingjing-before.db`，完整性检查为 `ok`。若上线检查失败，优先切回旧镜像；本次无数据库结构变更，不应盲目覆盖上线后新增数据。

建议后续分别确认：参考视频时长校验、百炼限流退避、Clash 候选节点切换，以及已成功视频的成品恢复。这四项不包含在本次超时调整中。
