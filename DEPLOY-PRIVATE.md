# 灵镜 · 私有化部署指南

私有化是灵镜的核心护城河:整套部署到客户机房,数据本地化,能力网关可换客户自有云账号。
本文档面向交付工程师。与托管部署同一套代码(见 `DEPLOY.md` / `DEPLOY-ALIYUN.md`),
差异全在 `.env` + 后台配置 + 媒体发布策略。

---

## 1. 部署形态

两容器,`docker compose` 一键起(与托管同):

```
┌──────────────────────────────────────────────┐
│ 客户机房 / 内网                                │
│  ┌────────┐   ┌─────────┐   ┌──────────────┐  │
│  │ Caddy  │──▶│  app    │──▶│ SQLite(卷)    │  │
│  │(HTTPS) │   │ (9372)  │   │ 对象存储(见 §3)│  │
│  └────────┘   └────┬────┘   └──────────────┘  │
│                    │ 能力网关(客户自有云账号 key)│
└────────────────────┼──────────────────────────┘
                     ▼
        阿里百炼 / 火山方舟 / Google(按需,用客户的 key)
```

数据(账号、形象、音色、作品、积分、审计)全部留在客户本地 SQLite + 对象存储,不出内网。
仅「生成调用」走云端厂商,且用客户自己的云账号。

## 2. 部署步骤

```bash
# 1. 拷贝代码 + 配置
cp .env.example .env && chmod 600 .env

# 2. 配 .env
#   LJ_DOMAIN=<内网域名 / 客户证书域名>
#   SUPERADMIN_PASS=<强密码>
#   MASTER_KEY=<openssl rand -base64 32>   # 厂商 key 加密入库
#   OSS_*=<客户的对象存储四项>             # 见 §3 存储方案
# 厂商 key(百炼/火山/Google,用客户自有账号)部署后在 /admin「厂商 / Key」配,不进 .env。
# 在线支付(可选):PUBLIC_BASE_URL=<平台对外基址>;商户号/密钥在 /admin「在线支付」配。
#   内网收不到微信/支付宝公网回调属预期形态 —— 服务端每 60s 主动查单入账,到账延迟 ≤60s;
#   出网需代理时配 WECHAT_PROXY / ALIPAY_PROXY(compose 已转发)。见 docs/PAYMENTS-RUNBOOK.md §7。

# 3. 一键起(校验 → build → up → 等健康)
./scripts/deploy.sh

# 4. 建机构 + 管理员
#   /admin 登录(admin / SUPERADMIN_PASS)→ 新建租户 → 开户
#   或脚本:DB_FILE=/data/lingjing.db npx tsx scripts/seed-demo.mjs
```

> 厂商 key:百炼可在 .env 种子;**火山方舟 / Google AI Studio 在 `/admin →「厂商 / Key」配**
> (AES-256-GCM 加密入库,用客户自有账号)。三家都可只在后台配、随时切换。

## 3. 私有化关键①:对象存储 + 素材公网可达(必读)

数字人 wan2.2-s2v 要求 `image_url` / `audio_url` **云端厂商可访问**。私有化内网下,素材在客户内网,
厂商云端访问不到。代码已把这步抽象成「媒体发布策略」(`src/gateway/media-publisher.ts`),
交付时按客户网络选:

| 方案 | 做法 | 适用 |
|---|---|---|
| **A(推荐)** | 客户用阿里云 OSS(或兼容 S3 的对象存储),配 `OSS_*`,公网/受限出网可达 | 客户允许素材走 OSS |
| **B** | 给内网存储配「仅出网到厂商域名」的反代/白名单,签名 URL 即可达 | 客户允许受限出网 |
| **C** | 素材临时上传到厂商文件接口,用其返回的临时 URL | 客户完全不开放出网 |

> 代码默认走「OSS 配齐即用其签名 URL」;未配 OSS 时回退本机存储(MinIO 客户端,仅开发用,
> 私有化不建议)。方案 B/C 在 `PrivatePublisher.publish()` 里按客户环境实现(已标 TODO)。
>
> 若客户要求**完全离线**(连厂商都不出网),需改为客户本地部署的 s2v/TTS 模型,不在本期范围——
> 届时能力网关换一个本地 provider 即可,业务代码无需改。

## 4. 私有化关键②:合规配置(政企必做)

后台(管理员 / 超管):
- **AI 生成标识**:默认开启,成品打「AI 合成」标识 + 元数据。监管要求,勿关。
- **交付模式**:设为 `private`,媒体发布走私有化策略。
- **操作审计**:默认记录登录/生成/成员变更/发放,本地留存可导出。

## 5. 与托管模式的差异(一套代码两种交付)

| 维度 | 托管 SaaS | 私有化 |
|---|---|---|
| 厂商 key | 平台账号 | 客户自有账号(/admin 配) |
| 计费 | 平台按量透传 | 可关闭计费 / 内部统计 |
| 存储 | 平台 OSS | 客户 OSS / 内网对象存储 |
| 素材公网 | 天然可达 | 媒体发布策略(§3) |
| 数据 | 平台多租户隔离 | 单客户独占部署 |

代码完全相同,差异全在 `.env` + 后台设置 + 媒体发布策略实现。

## 6. 备份与运维

- **数据备份**:`./scripts/backup.sh`(SQLite 在线一致备份,可 cron);对象存储另按客户方案备份。
- **更新**:`./scripts/deploy.sh`(git pull + 重建 + 健康等待);DB schema 用 `CREATE TABLE IF NOT EXISTS` 向前兼容。
- **监控**:`GET /healthz`;后台「平台运营」看队列/失败率/容量诊断。
