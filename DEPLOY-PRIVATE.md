# 灵镜 · 私有化部署指南

私有化是灵镜的核心护城河:整套部署到客户机房,数据本地化,能力网关可换客户自有云账号。
本文档面向交付工程师。

> 现状:架构已为私有化预留(能力网关凭证切换 + 媒体发布策略 + MinIO 同构存储)。
> 本期为「可私有化」级别;完整离线安装包为后续工作。

---

## 1. 部署形态

整套 = 3 个进程,docker-compose 一键起:

```
┌──────────────────────────────────────────┐
│ 客户机房 / 内网                            │
│  ┌─────────┐   ┌────────┐   ┌──────────┐  │
│  │  app    │──▶│ MinIO  │   │ SQLite   │  │
│  │ (9372)  │   │ (9000) │   │ (卷)     │  │
│  └────┬────┘   └────────┘   └──────────┘  │
│       │ 能力网关(客户自有云账号 key)      │
└───────┼────────────────────────────────────┘
        ▼
   阿里百炼(wan2.2-s2v / cosyvoice)
```

数据(账号、形象、音色、作品、积分、审计)全部留在客户本地 SQLite + MinIO,不出内网。
仅「生成调用」走百炼,且用客户自己的云账号。

## 2. 部署步骤

```bash
# 1. 拷贝代码 + 配置
cp .env.example .env

# 2. 配置(.env)
DASHSCOPE_API_KEY=<客户自有百炼 API Key>   # 关键:私有化用客户的 key,计费走客户账户
BAICHUAN_AVATAR_MODEL=wan2.2-s2v
BAICHUAN_TTS_MODEL=cosyvoice-v2
MINIO_ACCESS_KEY=<改成强密码>
MINIO_SECRET_KEY=<改成强密码>

# 3. 一键起
docker compose up --build -d

# 4. 建机构 + 管理员(进 app 容器或本地)
DB_FILE=/data/lingjing.db npx tsx scripts/seed-demo.mjs   # 或自定义机构名/账号
```

## 3. 私有化关键:素材公网可达问题(必读)

**问题**:wan2.2-s2v 要求 `image_url` / `audio_url` 公网可访问,但私有化时素材在客户内网 MinIO,
百炼云端访问不到。代码已把这一步抽象成「媒体发布策略」(`src/gateway/media-publisher.ts`),
交付时按客户网络三选一:

| 方案 | 做法 | 适用 |
|---|---|---|
| **A(推荐)** | 给 MinIO 配「仅出网到百炼域名」的反代/白名单,签名 URL 即可达 | 客户允许受限出网 |
| **B** | 素材临时上传到 DashScope 文件接口,用其返回的临时 URL | 客户完全不开放 MinIO 出网 |
| **C** | 客户提供一个临时公网 OSS 中转桶,素材推过去再给 URL | 客户已有公网 OSS |

默认实现是方案 A(直接用 MinIO 签名 URL,假设出网已放通)。方案 B/C 在
`PrivatePublisher.publish()` 里按客户环境实现(已标 TODO)。

> 若客户要求**完全离线**(连百炼都不出网),则需改为客户本地部署的 s2v/TTS 模型,
> 不在本期范围 —— 届时能力网关换一个本地 provider 即可,业务代码无需改。

## 4. 合规配置(政企必做)

系统设置(管理员):
- **AI 生成标识**:默认开启,成品右下角打「AI 合成」水印 + 元数据。监管要求,勿关。
- **交付模式**:设为 `private`,媒体发布走私有化策略。
- **操作审计**:默认记录登录/生成/成员变更/发放,本地留存可导出。

## 5. 与托管模式的差异(一套代码两种交付)

| 维度 | 托管 SaaS | 私有化 |
|---|---|---|
| 百炼 key | 平台账号 | 客户自有账号 |
| 计费 | 平台按量透传 | 可关闭计费 / 内部统计 |
| 存储 | 平台 MinIO/OSS | 客户内网 MinIO |
| 素材公网 | 天然可达 | 媒体发布策略(§3) |
| 数据 | 平台多租户隔离 | 单客户独占部署 |

代码完全相同,差异全在 `.env` + 系统设置 + 媒体发布策略实现。

## 6. 备份与运维

- **数据备份**:定期备份 MinIO 数据卷 + SQLite 文件(`/data/lingjing.db`)。
- **升级**:停 app 容器 → 拉新镜像 → `docker compose up -d`;DB schema 用 `CREATE TABLE IF NOT EXISTS` 向前兼容。
- **监控**:`GET /healthz` 健康检查。
